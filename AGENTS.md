# homebrew-tap

wwwyo の CLI ツールを Homebrew で配布する tap。この repo が持つのは formula だけで、ビルドも依存も生成ロジックも持たない。

## ディレクトリ構造

```
homebrew-tap/
└── Formula/
    ├── syokan.rb    # generated by wwwyo/syokan
    └── <tool>.rb    # 1 tool = 1 formula
```

## Formula は生成物であり、ここでは編集しない

各 formula は、そのツール自身の repo の release workflow が生成して push する。tap 側から見ると formula は常に「降ってくるもの」で、書き換える対象ではない。

一方向（ツール repo → tap）に閉じているのは、tap 側で手編集しても次の release が無言で上書きし、編集内容と、なぜそう直したかの文脈の両方が失われるため。formula の中身を変えたいときは、そのツール側の生成器を直す。

したがってこの repo で行う正当な変更は次に限られる:

- README.md / AGENTS.md の更新
- ツール側で生成器を直したうえでの、release 経由の formula 更新

## 新しいツールを追加する

tap 側での作業は無い。追加するツールの repo に次を用意すると、release のたびに formula がここへ push される。

1. cross-compile したバイナリを `<tool>-<os>-<arch>` の名前で GitHub Release に上げる
2. 同じ release に sha256 の一覧（`checksums.txt`）を含める
3. checksums.txt から formula を生成するスクリプトを置く（URL と sha256 を OS/arch ごとに埋める）
4. release workflow に「生成 → この repo を clone → `Formula/<tool>.rb` を差し替えて push」する job を足す
5. **この repo のみ / contents:write** に絞った fine-grained PAT を、そのツール repo の secret に置く

参照実装は syokan の [generate-formula.ts](https://github.com/wwwyo/syokan/blob/main/apps/syokan/scripts/generate-formula.ts) と [release.yml](https://github.com/wwwyo/syokan/blob/main/.github/workflows/release.yml)。

設計上の要点を 2 つ引き継ぐこと:

- **push job は publish の後段に置く** — formula 更新が失敗しても release 自体は成立させ、赤い job として顕在化させる。前段に置くと tap の一時的な失敗が release 全体を止める
- **formula が前回と同一内容なら commit を skip する**（`git diff --cached --quiet || git commit`）— job の再実行が「nothing to commit」で落ちなくなる

## 動作確認

配布物のみでツールチェーンを持たないため、install するものは無い。確認は Homebrew 側で行う。

```bash
brew tap wwwyo/tap
brew install wwwyo/tap/<formula>
brew test <formula>
```
